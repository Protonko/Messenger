export interface IUser {
  avatar: null | string
  confirm_hash?: string
  confirmed: boolean
  createdAt: string
  email: string
  full_name: string
  last_seen: string
  updatedAt: string
  id: string
}
